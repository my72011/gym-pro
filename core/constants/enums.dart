enum Gender { male, female }

enum Goal { lose_weight, maintain, build_muscle }

enum ExperienceLevel { beginner, intermediate, advanced }

enum WorkoutLocation { home, gym, both }

enum MuscleGroup { chest, back, legs, shoulders, arms, core, full_body }

enum Equipment { bodyweight, dumbbell, barbell, machine, cable, kettlebell }

enum WorkoutSplit { full_body, upper_lower, push_pull_legs, bro_split }